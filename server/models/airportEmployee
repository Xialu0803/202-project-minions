import mongoose from 'mongoose';

const createSchema = mongoose.Schema({
    employeeId: String,
    name: String,
});

const AirportEmployee = mongoose.model('AirportEmployee', createSchema);

export default AirportEmployee;