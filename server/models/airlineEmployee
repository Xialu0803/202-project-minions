import mongoose from 'mongoose';

const createSchema = mongoose.Schema({
    employeeId: String,
    airline: String,
    name: String,
});

const AirlineEmployee = mongoose.model('AirlineEmployee', createSchema);

export default AirlineEmployee;